# Brent-Lisp

A small experiment with writing an interpreter for a lisp-like language.

Example:

```scheme
; code.br
(define name "Brentarus")

(print name)
```

Planned features:

- `define`, `let`
- `lambda`

Todos:

- [ ] Tokenize
- [ ] Parse
- [ ] Evaluate

